<Window x:Class="UroborosApp.Utils.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Смена пароля" 
        Width="400"
        Height="250"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">

        <StackPanel>
            <TextBlock Text="Текущий пароль:" Margin="0 0 0 5" />
            <PasswordBox x:Name="OldPasswordBox" Margin="0 0 0 10" />

            <TextBlock Text="Новый пароль:" Margin="0 0 0 5" />
            <PasswordBox x:Name="NewPasswordBox" Margin="0 0 0 10" />

            <TextBlock Text="Подтвердите пароль:" Margin="0 0 0 5" />
            <PasswordBox x:Name="ConfirmPasswordBox" Margin="0 0 0 20" />
        </StackPanel>
        

        <StackPanel VerticalAlignment="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Отмена"  Click="Cancel_Click" />
            <Button Content="Сохранить" Click="SavePassword_Click" />
        </StackPanel>
    </Grid>
</Window>